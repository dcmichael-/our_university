<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ouruniversity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="12" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
  		android:configChanges="orientation|keyboard"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.example.unistportal.MainActivity"
            android:configChanges="keyboardHidden|orientation"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.unistportal.Boardview"
      android:configChanges="orientation|keyboard">
            <intent-filter>
                <action android:name="Data"/>
            </intent-filter>
        </activity>
        <activity android:name="com.nbpcorp.mobilead.sdk.MobileAdBrowserActivity"/>
    </application>

</manifest>